# Preenchido no Passo 2 (banco de dados) e Passo 3 (API do Claude).
# Copie este arquivo para ".env" e preencha os valores reais — o ".env" nunca vai pro repositório.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_ANTHROPIC_API_KEY=
